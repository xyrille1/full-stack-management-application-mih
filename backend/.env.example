# MongoDB connection string. Points at a local mongod instance by default.
MONGODB_URI=mongodb://127.0.0.1:27017/taskmanager

# Port the API listens on. The frontend dev server proxies /api here.
PORT=5000
